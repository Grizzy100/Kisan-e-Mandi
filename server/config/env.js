// server/config/env.js
// This file MUST be the very first import in server.js.
// In ES Modules, all imports are hoisted, so dotenv must be loaded
// via a dedicated file imported before anything else.
import "dotenv/config";
